<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TigersHutCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:colorBackground">@color/bgcolor_light</item>
        <item name="android:textColor">@color/fgcolor_dark</item>
    </style>
    <style name="Theme.TigersHutCalculator" parent="Base.Theme.TigersHutCalculator" />
</resources>